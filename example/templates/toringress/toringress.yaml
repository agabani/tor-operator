apiVersion: tor.agabani.co.uk/v1
kind: TorIngress
metadata:
  name: {{ include "example.fullname" . }}
  labels:
    {{- include "example.labels" . | nindent 4 }}
spec:
  onion_service:
    ports:
      - target: example:80
        virtport: 80
