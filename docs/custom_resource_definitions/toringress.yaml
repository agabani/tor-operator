apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: toringresses.tor.agabani.co.uk
spec:
  group: tor.agabani.co.uk
  names:
    categories: []
    kind: TorIngress
    plural: toringresses
    shortNames: []
    singular: toringress
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: The hostname of the tor ingress
      jsonPath: .status.hostname
      name: Hostname
      type: string
    - description: Number of replicas
      jsonPath: .status.replicas
      name: Replicas
      type: number
    - description: Human readable description of state
      jsonPath: .status.state
      name: State
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for TorIngressSpec via `CustomResource`
        properties:
          spec:
            description: |-
              A Tor Ingress is collection of Onion Services load balanced by a Onion Balance.

              The user must provide the Onion Key for the Onion Balance.

              The Tor Operator will auto generate random Onion Keys for the Onion Services.
            properties:
              onion_balance:
                description: Onion Balance settings.
                properties:
                  config_map:
                    description: Config Map settings.
                    nullable: true
                    properties:
                      name:
                        description: |-
                          Name of the Config Map.

                          Default: name of the Tor Ingress
                        nullable: true
                        type: string
                    type: object
                  deployment:
                    description: Deployment settings.
                    nullable: true
                    properties:
                      name:
                        description: |-
                          Name of the Deployment.

                          Default: name of the Tor Ingress
                        nullable: true
                        type: string
                    type: object
                  name:
                    description: |-
                      Name of the Onion Balance.

                      Default: name of the Tor Ingress
                    nullable: true
                    type: string
                  onion_key:
                    description: Onion Key settings.
                    properties:
                      name:
                        description: Name of the Onion Key.
                        type: string
                    required:
                    - name
                    type: object
                required:
                - onion_key
                type: object
              onion_service:
                description: Onion Service settings.
                properties:
                  config_map:
                    description: Config Map settings.
                    nullable: true
                    properties:
                      name_prefix:
                        description: |-
                          Name prefix of the Config Map.

                          Default: name of the Tor Ingress
                        nullable: true
                        type: string
                    type: object
                  deployment:
                    description: Deployment settings.
                    nullable: true
                    properties:
                      containers:
                        description: Containers of the Deployment.
                        nullable: true
                        properties:
                          tor:
                            description: Tor container.
                            nullable: true
                            properties:
                              resources:
                                description: Resources of the container.
                                nullable: true
                                properties:
                                  limits:
                                    description: Limits of the Resources.
                                    nullable: true
                                    properties:
                                      cpu:
                                        description: CPU quantity of the Limits.
                                        nullable: true
                                        type: string
                                      memory:
                                        description: Memory quantity of the Limits.
                                        nullable: true
                                        type: string
                                    type: object
                                  requests:
                                    description: Requests of the Resources.
                                    nullable: true
                                    properties:
                                      cpu:
                                        description: CPU quantity of the Requests.
                                        nullable: true
                                        type: string
                                      memory:
                                        description: Memory quantity of the Requests.
                                        nullable: true
                                        type: string
                                    type: object
                                type: object
                            type: object
                        type: object
                      name_prefix:
                        description: |-
                          Name prefix of the Deployment.

                          Default: name of the Tor Ingress
                        nullable: true
                        type: string
                    type: object
                  name_prefix:
                    description: |-
                      Name prefix of the Onion Service.

                      Default: name of the Tor Ingress
                    nullable: true
                    type: string
                  onion_key:
                    description: Onion Key settings.
                    nullable: true
                    properties:
                      name_prefix:
                        description: |-
                          Name prefix of the Onion Key.

                          Default: name of the Tor Ingress
                        nullable: true
                        type: string
                      secret:
                        description: Secret settings.
                        nullable: true
                        properties:
                          name_prefix:
                            description: |-
                              Name prefix of the Secret.

                              Default: name of the Tor Ingress
                            nullable: true
                            type: string
                        type: object
                    type: object
                  ports:
                    description: Onion Service Hidden Service ports.
                    items:
                      properties:
                        target:
                          description: The target any incoming traffic will be redirect to.
                          type: string
                        virtport:
                          description: The virtual port that the Onion Service will be using.
                          format: int32
                          type: integer
                      required:
                      - target
                      - virtport
                      type: object
                    type: array
                  replicas:
                    default: 3
                    description: Number of replicas.
                    format: int32
                    type: integer
                required:
                - ports
                type: object
            required:
            - onion_balance
            - onion_service
            title: Tor Ingress
            type: object
          status:
            nullable: true
            properties:
              hostname:
                description: |-
                  Onion key hostname.

                  The hostname is only populated once `state` is "running".
                nullable: true
                type: string
              label_selector:
                default: ''
                description: Label selector the Horizontal Pod Autoscaler will use to collect metrics.
                type: string
              replicas:
                description: Number of replicas.
                format: int32
                type: integer
              state:
                description: |-
                  Human readable description of state.

                  Possible values:

                  - onion balance onion key not found - onion balance onion key hostname not found - onion service onion key hostname not found - running
                type: string
            required:
            - replicas
            - state
            type: object
        required:
        - spec
        title: TorIngress
        type: object
    served: true
    storage: true
    subresources:
      scale:
        labelSelectorPath: .status.label_selector
        specReplicasPath: .spec.onion_service.replicas
        statusReplicasPath: .status.replicas
      status: {}
